interface AppScreensInterface {
  AUTH: string,
  SIGN_IN: string,
  SIGN_UP: string,
}

const SCREENS: AppScreensInterface = {
  AUTH: 'Auth',
  SIGN_IN: 'SignIn',
  SIGN_UP: 'SignUp',
}

export default SCREENS;
